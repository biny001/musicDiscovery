// import { createSelector } from "@reduxjs/toolkit";

// const selectArtist = (state) => state.artist.artist;
// const selectMusic = (state) => state.music.musicList;

// export const selectCurrentArtist = createSelector(
//   selectArtist,
//   (artist) => artist
// );
// export const selectCurrentMusic = createSelector(selectMusic, (music) => music);
